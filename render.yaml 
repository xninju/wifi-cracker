services:
  - type: web
    name: wifi-pentest
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
    regions:
      - frankfurt  # Choose closest
    plan: starter  # Free tier
    autoDeploy: true